# LLM API Keys (at least one required)
OPENAI_API_KEY=sk-proj-your-openai-key-here
CLAUDE_API_KEY=sk-ant-your-claude-key-here

# LLM Provider Selection (default: claude)
LLM_PROVIDER=claude  # or 'openai'

# Temporal Configuration (defaults work for docker-compose)
TEMPORAL_ADDRESS=temporal:7233
TEMPORAL_NAMESPACE=default
TASK_QUEUE=chat-queue

# API Configuration
PORT=8000
CORS_ORIGINS=*  # Comma-separated origins for production

# Documents Directory (absolute path to directory containing files to process)
# NOTE: On macOS, Docker Desktop needs explicit permission for some directories
# Desktop works by default, but ~/Documents may require permissions in:
# Docker Desktop > Settings > Resources > File Sharing
DOCUMENTS_DIR=~/Desktop

# Development Settings
PYTHONUNBUFFERED=1
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR
